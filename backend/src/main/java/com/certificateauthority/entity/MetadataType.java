package com.certificateauthority.entity;

/**
 * Enum representing metadata value types for proper serialization/deserialization
 */
public enum MetadataType {
    STRING,
    NUMBER,
    BOOLEAN,
    JSON
}